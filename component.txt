import { useProducts } from "../context/ProductContext";

const SomeComponent = () => {
  const { setRefreshTrigger } = useProducts();

  const refreshProducts = () => {
    setRefreshTrigger(prev => !prev); // 🔥 toggle
  };

  return (
    <button onClick={refreshProducts}>
      Refresh Products
    </button>
  );
};