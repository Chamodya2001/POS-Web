flask
flask-sqlalchemy
flask-migrate
flask-marshmallow
marshmallow
marshmallow-sqlalchemy
flask-jwt-extended
flask-cors
psycopg2-binary
python-dotenv
watchdog

# conda activate pos-env
# pip install -r requirements.txt

# Verify installed packages:

# pip freeze
